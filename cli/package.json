{
  "name": "trade-monitor-claude-cli",
  "version": "6.0.0",
  "private": true,
  "type": "module",
  "description": "회사 Claude Code CLI 기반 로컬 HTML 통상 모니터링",
  "scripts": {
    "monitor": "node run.mjs --open",
    "test": "node --test-reporter=dot test/claude-client.test.mjs && node --test-reporter=dot test/html.test.mjs && node --test-reporter=dot test/live-runner.test.mjs && node --test-reporter=dot test/runner.test.mjs && node --test-reporter=dot test/runtime-support.test.mjs",
    "check": "node --check run.mjs && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
