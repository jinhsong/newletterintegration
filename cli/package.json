{
  "name": "trade-monitor-claude-cli",
  "version": "5.0.0",
  "private": true,
  "type": "module",
  "description": "회사 Claude Code CLI 기반 로컬 HTML 통상 모니터링",
  "scripts": {
    "monitor": "node run.mjs --open",
    "test": "node --test",
    "check": "node --check run.mjs && node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
